[package]
name = "convert"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { version = "0.37.0", features = ["fbx"] }
protocol = { path = "../protocol" }
gltf-json = "1.4"
zip = "2.2"
image = "0.25"
